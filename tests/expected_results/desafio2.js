module.exports = [
	{
		"cancoes": 10,
		"artistas": 6,
		"albuns": 8
	}
];

/* DROP DATABASE IF EXISTS SpotifyClone;

  CREATE DATABASE IF NOT EXISTS SpotifyClone;

  CREATE TABLE SpotifyClone.plano_usuario(
      plano_id  INT PRIMARY KEY AUTO_INCREMENT,
      nome_do_plano VARCHAR(255) NOT NULL,
      valor_plano DOUBLE NOT NULL
  ) engine = InnoDB;

 INSERT INTO SpotifyClone.plano_usuario (nome_do_plano, valor_plano)
  VALUES
    ('gratuito', 0.00),
    ('familiar', 7.99),
    ('universitário', 5.99),
    ('pessoal', 6.99); */
